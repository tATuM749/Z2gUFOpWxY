version: "2"
checks:
  argument-count:
    enabled: true
  complex-logic:
    enabled: true
    config:
  file-lines:
    enabled: true
    config:
    config:
      threshold: 12
    enabled: true
  method-lines:
      threshold: 100
    config:
      threshold: 4
  return-statements:
    enabled: true
    config:
      threshold: 8
  similar-code:
  identical-code:
    enabled: false
# plugins:
#   eslint:

exclude_patterns:
  - "**/mock_*"
